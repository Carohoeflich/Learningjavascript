const reverse = str => str.split('').reverse().join(''); //.join joins the words in the right order, without it, it just outputs each word separately bc of .split

reverse('Word in reverse'); // outputs 'esrever ni droW'
